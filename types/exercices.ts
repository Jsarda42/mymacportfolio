export type Exercises = {
    id: number;
    exNumber: string;
    title: string;
    instruction: string;
    lockedCode: string;
    initialCode: string;
    setupCode: string;
    secretTest: string;
};